#!/bin/sh
# Strip Cursor agent trailers injected by IDE/cloud agents.
MSG_FILE=$1
[ -f "$MSG_FILE" ] || exit 0

sed -i '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$MSG_FILE"
sed -i '/^Co-authored-by:.*cursoragent@cursor\.com$/d' "$MSG_FILE"
sed -i '/^Made-with: Cursor$/d' "$MSG_FILE"
